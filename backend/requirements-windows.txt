# Dependências MÍNIMAS para rodar o backend BackOn em Windows (produção).
# Use este arquivo em vez do requirements.txt (que tem libs internas da Emergent).
#
# Instalar:
#   pip install -r requirements-windows.txt
#
# Versões deixadas SEM pin para o pip escolher o build compatível com
# a sua versão do Python (Python 3.11, 3.12, 3.13 ou 3.14).

fastapi
uvicorn[standard]
python-dotenv
pydantic
motor
pymssql
